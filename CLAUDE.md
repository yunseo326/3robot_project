# claude.md 재구성

날짜: 8월 9
상태: 진행중
날짜 형식2: 2026-08-09
주요태그: 구현 시도

# CLAUDE.md

시뮬레이션 전용 **소형 이족보행 캐릭터 로봇** 프로젝트의 진행 지시서.

> **이 파일의 역할**: 확정된 진행 방식만 적는다.
추정·가정·근거·미확인 사항은 이 파일에 쓰지 않는다. 아래 문서로 분리한다.
> 
> 
> 
> | 문서 | 내용 | 성격 |
> | --- | --- | --- |
> | `CLAUDE.md` (이 파일) | 파이프라인, 규칙, 통과 기준, 금지 사항 | **지시. 자주 안 바뀜** |
> | `docs/robot_spec.md` | 로봇 형태·관절 구성 명세 | 확정/미정 구분 |
> | `docs/assumptions.md` | 판단 근거, 추정, 미확인 사항, 검증 대기 목록 | **자주 바뀜** |
> | `docs/resources.md` | 외부 논문·저장소 조사 결과 | 사실 기록 |
> 
> 규칙이 바뀌어야 한다고 판단되면 CLAUDE.md를 직접 고치지 말고
> `logs/question.md`에 제안과 근거를 남긴다.
> 

---

## 프로젝트 개요

| 항목 | 내용 |
| --- | --- |
| 목표 | 소형 이족 캐릭터 로봇이 시뮬레이션 안에서 자연스럽고 캐릭터답게 움직이게 한다 |
| 형태 | 큰 LCD 머리 + 작은 몸통 + 짧고 두꺼운 평발 다리 + 뭉뚝한 팔 (`docs/robot_spec.md`) |
| 목표 동작 | ① 걷기 등 이동 ② 앉기·인사·고개갸웃 같은 트리거형 동작 |
| 환경 | 시뮬레이션 전용. 실기 배포 없음 |
| OS | WSL2 (Ubuntu 24.04) + conda |
| 시뮬레이터 | MuJoCo / MJX / MuJoCo Playground. LocoMuJoCo 사용 가능 |
| 금지 시뮬레이터 | Isaac Lab, Isaac Sim (사양 미달) |
| 연산 | `google-colab-cli`로 원격 T4 접속. 무료 티어만 |
| 작업자 | 학부 1학년 단독. PPO·보상설계·리타게팅 경험 있음
사족보행 모방학습 경험 있음(모방학습, 블렌더 → 클로드 코드 모델 성공) |
| 모드 | auto_mode. Claude Code가 자체 판단해 진행하고, 사용자 답변이 필요하면 `logs/question.md`에 남기고 나머지를 계속한다. 남길때는 상단에 요약본을 적고 하단에 본문을 적는다. 
답변이 끝난 내용은 history_question.md에 기록한고 question.md에서는 지운다
사용자가 보아야할 결과물과 result, question, progress 의 내용은 아티팩트도 추가적으로 작성 및 최신화한다. - 아티팩트를 구분해야할때에는 사용자에게 허락을 맡는다 |

---

## 현재 단계

작업 시작 전 이 블록을 읽고, STAGE 완료 시 갱신한다.

```
현재 STAGE : 1
다음 목표   : 1-A1 발목 유무 후보 A(발목 없음+롤오버 밑창)/B(발목 pitch) MJCF 작성 및 순수 RL 비교
차단 요인   : 없음 (단, robot_spec.md §9 절대 크기는 아직 미정 — STAGE 1 착수 전 확정 필요)
```

---

## STAGE 파이프라인

**원칙**

1. STAGE를 건너뛰지 않는다.
2. 각 STAGE는 이전 STAGE 산출물이 살아있는 상태에서 추가된다.
3. 통과 기준을 만족하지 못하면 다음 STAGE로 넘어가지 않는다.
4. 실패 시 다음 STAGE가 아니라 **이전 STAGE로 되돌아간다.**

| STAGE | 한 줄 목표 |
| --- | --- |
| 0 | 기성 모델로 스택이 도는지 확인하고, 우리 몸의 숫자를 측정한다 |
| 1 | 우리가 설계한 몸이 순수 RL로 걷는지 확인하고 DoF를 확정한다 ★최대 관문 |
| 2 | STAGE 1 결과를 레퍼런스로 모방학습 파이프라인을 검증한다 |
| 3 | 목을 정책 입력으로, 팔·얼굴을 정책 밖으로 분리한다 |
| 4 | 직접 만든 레퍼런스로 트리거 동작(앉기 등)을 학습한다 |
| 5 | 인간 모캡을 리타게팅해 학습한다 |
| 6 | 정책을 전환·통합한다 |

---

### STAGE 0 — 스택 검증 + 측정

**할 일**

1. 기성 소형 이족 모델 하나를 **수정 없이** MuJoCo Playground(MJX)에서 실행한다.
    - 1순위 `Robotis OP3` (Playground에 locomotion 환경 존재)
    - 2순위 `Berkeley Humanoid` (12 DoF, 최소 구성)
2. Colab T4에 접속해 학습을 돌린다.

| # | 측정 항목 | 방법 |
| --- | --- | --- |
| ① | 역진자 시간상수 `τ = √(무게중심높이 / g)` | 계산 |
| ② | 정책 제어 주파수 | 정책 스텝이 τ 안에 5회 이상 들어가도록 설정 |
| ③ | 고관절 최대 벌림각 | 다리를 벌려 허벅지끼리 닿는 각도를 시뮬에서 측정 |
| ④ | 명령 속도 상한 | `min(2·다리길이·sin(③)·스텝주파수, √(0.5·g·다리길이))` |
| ⑤ | 머리 접지 각도 | 몸통을 전·후·좌·우로 기울여 머리가 지면에 닿는 각도 4개 |

**하지 않을 것**: 캐릭터 디자인, 커스텀 MJCF 작성, 보상 설계

**통과 기준**

- 걷는 영상이 나온다
- 위 5개 숫자가 `logs/progress.md`에 기록되어 있다
- Colab 세션을 끊었다 붙여도 체크포인트가 남아 있다

---

### STAGE 1 — 다리 골격 + 몸 비율 탐색 + 순수 RL 걷기 ★최대 관문

**이 STAGE 전체의 원칙**
가장 먼저 실행할 것 
robot spec 에 나와있는 기본 조건을 먼저 실행해서 테스트 해볼것. 이후 사용자가 희망하는 선에서 다음 stage 및 내용 진행 

다리의 관절 구성과 몸의 비율은 **미리 정해진 정답이 없다.** 실험으로 하나씩 확정해나간다.
단, 한 번에 여러 축을 동시에 바꾸면 실패 원인을 알 수 없으므로 **한 번에 축 하나만 실험**한다.

```
이 시점에 팔·목·LCD 머리는 전부 "질량만 있는 고정 링크"로 둔다. 관절을 만들지 않는다.
STAGE 1이 답하는 질문은 오직 하나: "이 몸(다리 관절 구성 + 비율 + 형상)만으로 넘어지지 않고 걸을 수 있는가?"
```

**STAGE 1에서 실험하는 세 종류**

| 종류 | 무엇을 정하는가 | 방법 | 문서 위치 |
| --- | --- | --- | --- |
| A. 관절 유무 | 발목·hip yaw 등 관절을 넣을지 말지 | 후보 2개 A/B 비교 | `robot_spec.md` §4-A |
| B. 몸 비율 | 머리·몸통·다리 길이 비율 (범위: 머리 30~60%, 몸통·다리 각 20~40%) | 10%p 간격 그리드 스윕 | `robot_spec.md` §4-B |
| C. 다리·발 형상 | 다리 두께(다리길이의 15~35%), 발 면적(다리길이의 20~50%) | 10%p 간격 그리드 스윕 | `robot_spec.md` §4-C |

**진행 순서: A → B → C.** 관절 구성이 안 정해진 채 비율을 스윕하면 "비율 때문에 안 걷는지 관절 때문에 안 걷는지" 구분이 안 되므로 관절부터 고정한다. 마찬가지로 다리 두께·발 면적(C)은 몸 비율(B)이 먼저 정해진 상태에서 스윕한다.

각 서브스테이지는 다음 공통 절차를 따른다:

1. 이전 서브스테이지에서 확정된 구성을 **그대로 고정**한다.
2. 이번 서브스테이지가 다루는 축만 후보로 나눠 MJCF를 만든다 (A는 2개, B·C는 그리드 지점당 1개).
3. 각 후보를 **완전히 동일한 조건**으로 순수 RL 학습한다 (동일 보상·하이퍼파라미터·시드 개수·스텝 수).
보상은 §보상 구조 "튜닝 순서" 1~5번까지만 쓴다 (추종·스타일 보상 없음).
4. 비교표를 작성한다: 에피소드 길이 곡선 / 전진 속도 / 넘어짐 빈도 / 발-발 충돌 빈도
5. 승자를 확정하고 다음 서브스테이지로 넘어간다.

**서브스테이지 상세 목록**: `docs/robot_spec.md` §4를 따른다 (1-A1 발목 → 1-A2 hip 축 → 4-B 몸 비율 → 4-C1 다리 두께 → 4-C2 발 면적). 실험 도중 새로운 축이 필요하다고 판단되면 목록에 추가하고 `logs/question.md`에 근거를 남긴다.

**STAGE 1 확정 후 규칙**

```
모든 서브스테이지를 통과하면 다리 DoF 구성과 몸 비율이 확정된다.
확정된 값은 이후 절대 변경하지 않는다.
변경하면 그때까지 만든 모든 레퍼런스(npz)를 폐기해야 한다.
확정 결과는 models/character.xml 로 복사하고, 이후 이 파일만 쓴다.
```

**STAGE 1 통과 기준**

- 마지막 서브스테이지의 승자 구성으로 넘어지지 않고 전진한다
- 에피소드 길이가 최대치에 도달해 유지된다
- 모든 서브스테이지의 비교표가 `docs/robot_spec.md`에 채워져 있다

**실패 시**: 어느 서브스테이지에서 막혔는지 먼저 확인한다.

- 첫 서브스테이지(1-A1)부터 아무 후보도 안 걸으면 → STAGE 0으로 돌아가 ③④⑤ 측정값을 다시 확인한다. 명령 속도가 기하학적으로 불가능한 값이 아닌지 먼저 본다.
- 중간 서브스테이지에서 막히면 → 그 축의 후보들을 다시 설계한다. 이전 서브스테이지 확정 사항은 건드리지 않는다.
- 4-B(비율)나 4-C(형상) 단계에서 그리드 전체가 다 안 걸으면 → 4-A(관절 구성)로 돌아가 다른 조합을 재검토한다.

---

### STAGE 2 — 모방학습 파이프라인 검증

**할 일**

1. STAGE 1의 학습된 정책을 실행해 매 프레임 상태를 녹화한다 → `references/stage1_walk.npz`
2. 그 npz를 레퍼런스로 모방학습을 돌린다.
3. §보상 구조의 전체 항목(imitation + regularization + limits + impact)을 투입한다.

**이 데이터를 쓰는 이유**: STAGE 1 결과는 물리 시뮬레이터 안에서 이미 실현된 동작이다. 즉 "레퍼런스가 물리적으로 가능한가"라는 변수가 제거된 상태다. 여기서 실패하면 **원인은 100% 모방학습 파이프라인 쪽**으로 확정된다.

**통과 기준**: reward가 정체 없이 상승한다

**실패 시**: 레퍼런스를 의심하지 않는다. RSI/ET 설정, 보상 가중치, 관측 공간 매핑을 순서대로 점검한다.

---

### STAGE 3 — 표현 채널 분리

**할 일**

1. **목을 정책의 조건 입력으로 넣는다.**
    - 정책 입력에 목표 목관절각 `q̂_neck`을 추가한다
    - 학습 중 `q̂_neck`을 전 범위에서 무작위화한다
    - 보상에 `imitation/pose_neck`을 추가하고 다리와 **가중치를 분리**한다
2. **팔과 LCD 표정을 정책에서 뺀다.**
    - `scripts/show_function.py`에 PD/키프레임 구동을 구현한다
    - RL 정책은 다리 + 목만 제어한다
3. 팔 질량 비중을 측정한다. 전체의 5%를 넘으면 정책에 포함시키되 추종 가중치를 낮게 준다.
    - 판정 방법: 팔을 흔드는 조건과 고정한 조건으로 같은 걷기 정책을 돌려 에피소드 길이 차이를 본다

**결과**: "인사(손흔들기)"는 서기 정책 위에 팔 애니메이션을 얹으면 끝난다. RL을 따로 돌리지 않는다.

**통과 기준**

- 목 각도를 임의로 지정해도 넘어지지 않는다
- 팔을 흔들어도 걷기가 유지된다

---

### STAGE 4 — 트리거 동작 레퍼런스 제작

**할 일**

1. IK(`scripts/ik_gait.py`) 또는 퍼펫티어링(`scripts/puppeteer.py`)으로 레퍼런스를 만든다.
2. **§레퍼런스 사전검사 ①~⑤를 통과시킨다.** 통과 못하면 RL을 돌리지 않는다.
3. 동작 하나씩 별도 정책으로 학습한다.

**순서**: 앉기 → 그 다음 동작. 시작/끝 자세가 명확하고 이동이 없어 판단이 쉽다.

**앉기 전 필수 확인**: 짧은 다리 + 큰 머리에서 앉으면 머리가 무릎·허벅지에 부딪힐 수 있다. 목표 자세를 정적으로 놓아보고 자기충돌을 먼저 확인한다.

**통과 기준**: 사전검사 통과 후 학습 → 넘어지지 않고 해당 동작을 수행한다

---

### STAGE 5 — 인간 모캡 리타게팅

**할 일**

1. LocoMuJoCo robot-to-robot 리타게팅 또는 GMR로 인간 모션을 우리 골격에 옮긴다.
2. **비율을 두 축으로 조정한다.**
    
    ```
    공간축: 위치 목표에 k = L_로봇 / L_인간 을 곱한다시간축: √k 를 곱한다 (시간을 압축한다 = 더 빠르게)
    ```
    
3. 발목이 없다면 리타게팅 목표에서 **발 자세 항을 제외하고 발 위치만 맞춘다.**
4. 사전검사 ①~⑤를 다시 돌린다.
5. 관절 속도 사전검사(②)에 걸릴 때만 시간축 압축을 완화한다. 처음부터 임의로 느리게 만들지 않는다.

**사용 범위**: 인간 모캡에서 실제로 전이되는 것은 루트 궤적과 다리 사이클뿐이다. 발·척추·팔 부분은 버린다.

**통과 기준**: STAGE 2와 비슷한 수준으로 추종한다

---

---

### STAGE 6 — 통합

**할 일**

1. 정책을 동작별로 유지한 채 전환 로직을 만든다.
2. path frame 개념으로 전역 자세 불변성을 확보한다.
3. 전환 구간에 블렌딩(0.2~0.3초)을 넣는다.

**통과 기준**: 걷기 ↔ 서기 ↔ 앉기 전환이 끊기지 않는다

---

## 보상 구조

```
r = imitation + regularization + limits + impact
```

### imitation

몸통 xy 위치 / 몸통 자세 / 선속도 xy / 선속도 z / 각속도 xy / 각속도 z /
**다리 관절각** / **목 관절각** / **다리 관절속도** / **목 관절속도** /
발 접촉 일치 `𝟙[c = ĉ]` / 생존

- 다리와 목의 가중치를 **반드시 분리한다.**

### regularization

토크 / 관절 가속도 / 다리 action rate / 목 action rate / 다리 action acc / 목 action acc

- 1차 차분(rate)과 2차 차분(acc)을 **둘 다** 쓴다. 1차만 쓰면 느린 톱니파를 학습한다.

### limits

관절 한계 하한 / 관절 한계 상한 / **발-발 충돌** / (필요 시 자기충돌)

- 관절 한계는 단순 각도 페널티가 아니라 CBF 형태로 준다: 한계에서 여유 `q_m`을 두고, 한계에 가까워질 때 그쪽으로 가는 속도에 벌점.
- 시작값: `q_m = 0.1 rad`, `γ_q = 20`

### impact

발의 중력 방향 속도 변화 `Δv_z`에 페널티.

- **반드시 saturate 한다.** 접촉 해석에서 나오는 큰 속도 변화가 critic 학습을 불안정하게 만든다.

### 종료 조건

머리·몸통·허벅지·팔이 지면에 닿으면 즉시 종료.

### 튜닝 순서

```
1. 과제 보상만 (전진 + 생존)
2. 종료 조건 조이기
3. 큰 정규화 (수직 속도, 롤/피치, 충돌)
4. 토크/파워
5. action rate → action acc
6. 추종/스타일 보상 투입
7. 제약(CaT)으로 감싸기
```

- **6번을 1번과 동시에 하지 않는다.** 스타일 문제인지 과제 문제인지 구분되지 않는다.
- STAGE 1은 1~5번까지만 쓴다.

---

## 캐릭터 결 만들기

| 원하는 결 | 방법 |
| --- | --- |
| 유연함 | action rate·acc 페널티를 강하게, impact reduction 보상 추가 |
| 삐꺽거림 | **페널티는 그대로 두고, 레퍼런스 쪽에 의도적인 정지(hold)와 급전환(snap)을 넣는다** |

**스무딩 페널티를 끄지 않는다.** 페널티를 끄면 나오는 것은 삐꺽거림이 아니라 떨림(jitter)이고, 떨림은 캐릭터가 아니라 버그로 보인다. 로봇다운 각진 움직임은 레퍼런스 애니메이션의 이징 곡선으로 만들고, 정책은 그걸 부드럽게 추종한다.

---

## 작업 규칙

### 1. 한 번에 한 변수만 바꾼다

동시에 두 가지를 바꾸면 실패 원인을 특정할 수 없다.

### 2. 로깅은 항목별로 쪼갠다

```python
# ✗ 금지
log("reward", total_reward)

# ✓ 필수
log("reward/imitation/root")
log("reward/imitation/pose_leg")
log("reward/imitation/pose_neck")
log("reward/imitation/contact")
log("reward/regularization")
log("reward/limits/joint")
log("reward/limits/foot_collision")
log("reward/impact")
log("episode_length")               # ★ 가장 중요한 진단 지표
```

**진단 규칙**

| 증상 | 원인 |
| --- | --- |
| 에피소드 길이가 짧고 안 늘어남 | 계속 넘어짐 = 레퍼런스 현실성 또는 몸 설계 문제 |
| 길이는 최대인데 reward만 정체 | 추종 실패 = 토크 부족 또는 매핑 문제 |
| `imitation/root`만 낮음 | 균형 붕괴 |
| `imitation/pose_leg`만 낮음 | 토크 또는 매핑 문제 |
| `imitation/pose_neck`만 낮음 | 목 액추에이터가 머리 질량을 못 버팀 → 머리 질량 또는 기어비 재검토 |
| `limits/foot_collision`이 계속 걸림 | 스탠스 폭 확대 |

### 3. 체크포인트

```
SAVE_DIR = 'checkpoints/[stage]/[stage내 시도번호]/[목적.학습스텝.날짜]'
```

- 저장 주기는 20만 스텝 단위.

### 4. 레퍼런스 사전검사 (STAGE 4·5·6 필수)

`scripts/check_feasibility.py`에 구현. **비용이 싼 순서대로 돌린다.**

| # | 검사 | 물리 시뮬 |
| --- | --- | --- |
| ① | 지지 다각형 — 닿은 발들의 다각형 안에 무게중심이 있는가 | 불필요 |
| ② | 관절 속도·가속도 — 프레임 간 변화가 액추에이터 한계 안인가 | 불필요 |
| ③ | 발 관통 — 발 높이가 지면 아래로 내려가는 프레임이 있는가 | 불필요 |
| ④ | 발 자세 도달 가능성 — 접지 프레임에서 요구되는 발 자세가 실현 가능한가 | 불필요 |
| ⑤ | Kinematic replay — 물리를 켜고 각 자세를 놓아봤을 때 무너지는가 | 필요 (학습은 불필요) |

①~④는 레퍼런스 파일만으로 즉시 계산된다. 통과 못하면 RL을 돌리지 않는다.

### 5. 정책은 동작별로 분리한다

```
✓ 서기 정책 / 걷기 정책 / 앉기 정책 — 각각 별도
✗ 하나의 정책으로 통합
```

---

## 금지 사항

### 사용 금지

| 항목 | 사유 |
| --- | --- |
| Isaac Lab / Isaac Sim | 사양 미달 |
| 개 mocap 리타게팅 (Zhang 2018, Peng 2020) | 4족 데이터. 이족에 부적합 |
| Humanoid-R0 / RLPF / SafeFlow | 코드 미공개 + 철회된 제출물 |
| RobotMDM | 코드 미확인. 개념만 참고 |
| 파운데이션 모델 / flow·diffusion 정책 | 데이터 규모가 학부 단독 범위 밖 |
| 궤적최적화 (TOWR/Crocoddyl/OCS2) | IK로 충분. 오버엔지니어링 |

### 조건부 허용 (조건 충족 전 착수 금지)

| 항목 | 조건 |
| --- | --- |
| MDM / MoMask / T2M-GPT / QuickMagic | STAGE 5 완료 후 |
| AMP | 리타게팅 모션 30분 이상 확보 후 + 사용자 허가 |
| z공간 / VAE 모션 프라이어 | 동작 10개 이상 축적 후 + 사용자 허가 |
| AI 영상 생성 (image-to-video) | STAGE 6 완료 후 + 사용자 허가 |

### 프로세스 금지

- **여러 동작을 동시에 테스트하지 않는다.** 개별 검증 완료 후에만 조합한다.
- **reward 총합만 보고 판단하지 않는다.** 항목별로 쪼개서 본다.
- **팔·LCD 얼굴을 RL 정책에 넣지 않는다.** (STAGE 3 기준. 팔 질량이 5%를 넘는 경우만 예외)
- **STAGE 0에서 캐릭터 디자인을 하지 않는다.**
- **DoF 확정 후 관절을 추가하지 않는다.**
- CLAUDE.md를 임의로 수정하지 않는다. 제안은 `logs/question.md`에 남긴다.

---

## 디렉토리 구조

```
.
├── CLAUDE.md
├── docs/
│   ├── robot_spec.md            # 로봇 형태·관절 명세
│   ├── assumptions.md           # 근거·추정·미확인 (자주 갱신)
│   |── resources.md             # 외부 논문·저장소 조사
│   └── reference            # 외부 로봇 참고 그림
├── models/
│   ├── candidate_A.xml          # STAGE 1 후보
│   ├── candidate_B.xml          # STAGE 1 후보
│   └── character.xml            # STAGE 1 종료 후 확정본 (이후 고정)
├── references/
│   ├── stage1_walk.npz
│   ├── ik_walk.npz
│   ├── puppet_sit.npz
│   └── retarget_*.npz
├── scripts/
│   ├── check_feasibility.py     # 사전검사 ①~⑤
│   ├── record_policy.py         # 정책 → npz 녹화
│   ├── ik_gait.py               # IK 보행 레퍼런스
│   ├── puppeteer.py             # 키보드 조종 + 녹화
│   ├── retarget.py              # 인간 모캡 → 우리 골격 (비율·시간 스케일 포함)
│   └── show_function.py         # 팔·LCD 애니메이션 (RL 밖)
├── envs/
│   ├── biped_rl.py              # STAGE 1
│   └── biped_mimic.py           # STAGE 2+
├── policies/
│   ├── stand/
│   ├── walk/
│   └── sit/
└── logs/
    ├── checkpoints/             # 학습 체크포인트
    ├── result/                  # 영상·사진·보고서
    ├── question.md              # 사용자 답변이 필요한 것
    ├── history_question.md              # 사용자 답변이 끝나서 필요없는것
    └── progress.md              # 진행사항 + STAGE 0 측정값
```

---

## 표기 규칙

```
【기업】회사    《논문》논문    〈기술〉기법    ▣프로그램    ⌘코드저장소
```

---

## 응답 방식

- 결과물 요청 시 아티팩트로 제시한다
- 수식보다 개념과 원리 중심으로 설명한다
- 확실하지 않으면 추측하지 말고 모른다고 말한다
- 코드 공개 여부는 실제 확인 여부를 구분해 표기한다
- 선택지를 제시할 때는 우선순위와 근거를 함께 제시한다